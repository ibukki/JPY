var jpy = window.jpy || {};

jpy.paramLoader = {
	serviceUrl: "",
	loadParams: function(xargs){
		var schemaId = xargs.schemaId;
		if(xargs.type == "input"){
			
		}else if(xargs.type == "output"){
			
		}
	}	
};